fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# PDF handling
PyMuPDF==1.24.10          # fast text extraction + page rasterization (fitz)
pdf2image==1.17.0         # fallback page->image conversion (needs poppler installed)
pdfplumber==0.11.4        # table extraction for geological/assay tables

# OCR for scanned/image-based PDFs
pytesseract==0.3.13       # needs system tesseract-ocr installed
Pillow==10.4.0

# NLP / embeddings / retrieval
sentence-transformers==3.1.1
faiss-cpu==1.8.0.post1
numpy==1.26.4
scikit-learn==1.5.2

# Local AIML question-answering model (no external API calls)
torch==2.4.1
transformers==4.44.2

# Domain term matching
regex==2024.9.11
